---
'pesan-ai': minor
---

Implement history sync on embedded sign up service, webhook handler for `history` and `smb_app_state_sync` webhooks, add loading UI component, and reorganize some repositories code
