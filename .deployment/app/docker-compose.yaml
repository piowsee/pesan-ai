services:
  app:
    container_name: pesanai_app
    image: piowsee/pesanai:latest
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - backend

networks:
  backend:
    external: true
    name: pesanai_backend
