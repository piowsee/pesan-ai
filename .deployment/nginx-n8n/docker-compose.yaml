services:
  nginx_n8n:
    image: nginx:latest
    container_name: nginx_n8n
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - '80:80'
    networks:
      - backend

networks:
  backend:
    external: true
    name: n8n_backend
